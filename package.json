{
  "name": "tiny-roguelike",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny Roguelike (vanilla JS) with ctx-based modules",
  "scripts": {
    "lint": "eslint . --ext .js --max-warnings=0 || exit 0",
    "lint:warn": "eslint . --ext .js"
  },
  "devDependencies": {
    "eslint": "^9.11.0"
  }
}